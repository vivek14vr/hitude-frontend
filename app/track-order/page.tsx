import type { Metadata } from 'next';
import { Search, ShieldCheck } from 'lucide-react';
export const metadata: Metadata = { title: 'Track order', description: 'Track a HITUDE order securely.', robots: { index: false, follow: false } };
export default function TrackOrderPage() { return <div className="page-shell flex min-h-[70vh] items-center justify-center py-16"><div className="w-full max-w-lg text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-cream text-moss"><Search size={23} /></div><p className="eyebrow mt-8">Order tracking</p><h1 className="mt-4 font-display text-5xl">Where is your order?</h1><p className="mt-5 text-[15px] leading-7 text-ink/60">Enter the order number from your confirmation email. We’ll only show information tied to that order.</p><form className="mx-auto mt-8 flex max-w-md gap-2"><label className="sr-only" htmlFor="tracking-number">Order number</label><input required id="tracking-number" className="input" placeholder="#HT-0000" /><button className="button button-primary" type="submit">Track</button></form><p className="mt-6 flex items-center justify-center gap-2 text-xs text-moss"><ShieldCheck size={14} /> Secure lookup · no account required</p></div></div>; }

