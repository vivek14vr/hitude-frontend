import type { Metadata } from 'next';
import Link from 'next/link';
import { ArrowRight, Clock3 } from 'lucide-react';
import { posts } from '@/lib/product-data';
export const metadata: Metadata = { title: 'Learn / journal', description: 'HITUDE education on Vijaya, ingredients, labels, and responsible wellness rituals.', alternates: { canonical: '/blog' } };
export default function BlogPage() { return <div className="page-shell pb-24 pt-16 sm:pt-24"><div className="max-w-2xl"><p className="eyebrow">The HITUDE journal</p><h1 className="mt-5 font-display text-5xl leading-[.98] sm:text-7xl">Good questions<br /><span className="text-vermillion">welcome.</span></h1><p className="mt-7 max-w-lg text-[17px] leading-8 text-ink/62">A growing library of clear, useful notes to help you understand the plant, the product, and the ritual.</p></div><div className="mt-14 grid gap-5 md:grid-cols-3">{posts.map((post, index) => <Link className="group rounded-3xl border border-line p-6 transition hover:-translate-y-1 hover:bg-cream sm:p-8" href={`/blog/${post.slug}`} key={post.slug}><div className={`flex h-40 items-end rounded-2xl p-4 ${index === 0 ? 'bg-[#d7e1d5]' : index === 1 ? 'bg-[#ead5cb]' : 'bg-[#f0d8bd]'}`}><span className="font-display text-5xl text-ink/20">0{index + 1}</span></div><p className="eyebrow mt-6">{post.category}</p><h2 className="mt-3 font-display text-2xl leading-tight group-hover:text-vermillion">{post.title}</h2><p className="mt-3 text-sm leading-6 text-ink/60">{post.excerpt}</p><div className="mt-7 flex items-center gap-2 text-xs text-ink/45"><Clock3 size={14} /> {post.readTime}<ArrowRight className="ml-auto transition group-hover:translate-x-1" size={15} /></div></Link>)}</div><div className="mt-20 rounded-3xl bg-ink p-7 text-paper sm:p-10"><p className="eyebrow text-paper/50">A useful reminder</p><p className="mt-4 max-w-2xl font-display text-3xl leading-tight sm:text-4xl">Education is not a disclaimer. It’s part of the product.</p></div></div>; }

